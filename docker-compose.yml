version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app                 # Maps your code (Good)
      - /app/node_modules
    restart: unless-stopped
    environment:
      - NODE_ENV=development
