version: '3.8'

services:
  # 0. CADDY — TLS reverse proxy (auto-ACME, subdomain routing)
  caddy:
    container_name: camporee-caddy
    image: caddy:2-alpine
    ports:
      - "80:80"
      - "443:443"
    env_file: .env
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
    depends_on:
      - landing
      - composer
      - collator
      - demo-collator
    restart: unless-stopped

  # 1. LANDING — standalone marketing site + early-access form (camporeeconductor.com)
  landing:
    container_name: camporee-landing
    build:
      context: .
      dockerfile: Dockerfile.landing
    ports:
      - "3002:3002"   # Direct access for local dev
    environment:
      - PORT=3002
      - SUBMISSIONS_PATH=/app/data/submissions
    volumes:
      - ${DATA_DIR:-./data}/landing:/app/data/submissions
    restart: unless-stopped

  # 2. THE COLLATOR (Scorer) - Port 3000
  collator:
    container_name: camporee-collator
    build: .
    env_file: .env
    ports:
      - "3000:3000"   # Keep direct access for local dev / Composer
    environment:
      - PORT=3000
      - ACTIVE_SERVICES=collator     # <--- Identity Assignment
      - EVENT_PATH=/app/data/active-event
      - LIBRARY_PATH=/app/data/library
      - CONDUCTOR_DB_PATH=/app/data/shared/conductor.db
    volumes:
      - ${DATA_DIR:-./data}/collator:/app/data
      - ${DATA_DIR:-./data}/curator:/app/data/library
      - ${DATA_DIR:-./data}/shared:/app/data/shared
      - ./migrations:/app/migrations
      - ./src:/app/src
      - ./views:/app/views
      - ./public:/app/public
      - ./server.js:/app/server.js
    restart: unless-stopped

  # 3. THE COMPOSER & CURATOR (Builder/Library) - Port 3001
  composer:
    container_name: camporee-composer
    build: .
    env_file: .env
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - ACTIVE_SERVICES=curator,composer  # <--- Identity Assignment
      - WORKSPACE_PATH=/app/data/workspaces
      - LIBRARY_PATH=/app/data/library
      - CONDUCTOR_DB_PATH=/app/data/shared/conductor.db
    volumes:
      - ${DATA_DIR:-./data}/composer/workspaces:/app/data/workspaces
      - ${DATA_DIR:-./data}/curator:/app/data/library
      - ${DATA_DIR:-./data}/shared:/app/data/shared
      - ./migrations:/app/migrations
      - ./scripts:/app/scripts
      - ./src:/app/src
      - ./views:/app/views
      - ./public:/app/public
      - ./server.js:/app/server.js
    restart: unless-stopped

  # 4. THE DEMO COLLATOR — shared open demo (demo.camporeeconductor.com)
  demo-collator:
    container_name: camporee-demo-collator
    build: .
    env_file: .env
    ports:
      - "3003:3003"   # Direct access for local dev
    environment:
      - PORT=3003
      - ACTIVE_SERVICES=collator
      - DEMO_MODE=true
      - COLLATOR_MODE=offline
      - EVENT_PATH=/app/data/active-event
      - LIBRARY_PATH=/app/data/library
      # Path to the Circus cartridge zip inside the container.
      # Place the file at ${DATA_DIR}/demo-collator/cartridge.zip on the host.
      - DEMO_CARTRIDGE_PATH=/app/data/cartridge.zip
    volumes:
      - ${DATA_DIR:-./data}/demo-collator:/app/data
      - ${DATA_DIR:-./data}/curator:/app/data/library
      - ./src:/app/src
      - ./views:/app/views
      - ./public:/app/public
      - ./server.js:/app/server.js
    restart: unless-stopped

volumes:
  caddy_data:
